将注册功能按请求次数分步理解

1.点击注册按钮，跳转到注册页面，普通的get网页请求
2.输入信息后提交给服务器，表单post请求
3.服务器对信息进行查询，如果没问题，将信息存入数据库，并且进行密码加密，账号状态设置，激活码生成，发送激活邮件给注册邮箱，跳转到状态页面
   如果有问题，将问题Msg传给浏览器显示，跳转到注册页面
4.激活链接，通过@PathVariable 获取/后的数据，查询数据库，与用户名和激活码对比，跳转到状态页面（激活成功，激活失败，重复激活）