帖子分页

后端：分页时需要计算出页码栏显示的页码，每次处理请求都要获取当前页码Current，通过current计算出当前页帖子数据的起始行和终止行，
返回给数据库查询
	通过当前页码计算出显示的页码为current-2 到current +2,加入if边界值判断

前端：翻页时通过地址栏后加？current =   ；服务器获取get请求中的数据，即可知道当前页码
	当前页码高亮，如为首页，则上一页变灰，如为末页，则下一页变灰
	以上功能都为动态html，使用Thymeleaf模板引擎实现