忘记密码功能

如果邮箱不存在，网页显示该账号不存在

但是后端返回之后，页面并不会刷新    ----------------------------------------------------------已解决


不会刷新的原因为，当时用jQuery中的$.get（）发送请求时，应使用function（data）作为回调函数，获得服务器传回的数据

如果服务器正常return String ，那么data中的数据为 template组装好的html文本，无法显示

所以应该使用json传输一个code给前端，前端判断之后，在前端使用jQuery改变网页的显示内容

//异步请求